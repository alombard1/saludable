import { Button } from "../index";

const WhatsApp = () => {
  return (
    <div className="fixed right-4 bottom-4 z-10 md:right-10 md:bottom-10">
      <Button type="accent" icon="whatsapp" onClick={true} />
    </div>
  );
};

export default WhatsApp;
